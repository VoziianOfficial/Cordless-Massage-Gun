(function(){
  'use strict';
  const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];
  const header=q('.site-header');addEventListener('scroll',()=>header?.classList.toggle('is-scrolled',scrollY>8),{passive:true});
  if(header&&!q('[data-menu-toggle]')){const brand=q('.brand',header),button=document.createElement('button');button.className='brand__menu';button.type='button';button.dataset.menuToggle='';button.setAttribute('aria-expanded','false');button.setAttribute('aria-controls','mobile-menu');button.setAttribute('aria-label','Open menu');button.append(...[1,2,3].map(()=>document.createElement('span')));if(brand){const wrap=document.createElement('div');wrap.className='brand-shell';brand.before(wrap);wrap.append(button,brand)}if(!q('#mobile-menu')){const nav=document.createElement('nav');nav.className='mobile-menu';nav.id='mobile-menu';nav.setAttribute('aria-label','Mobile');(window.SITE_CONFIG.navigation||[]).forEach(item=>{const a=document.createElement('a');a.href=item.href;a.textContent=item.label;nav.append(a)});header.after(nav)}}
  const menu=q('.mobile-menu'),menuBtn=q('[data-menu-toggle]');menuBtn?.addEventListener('click',()=>{const open=menu.classList.toggle('is-open');menuBtn.setAttribute('aria-expanded',String(open))});qa('.mobile-menu a').forEach(a=>a.addEventListener('click',()=>menu?.classList.remove('is-open')));
  const badge=()=>{const count=window.PulsoCart?.count()||0;qa('[data-cart-count]').forEach(x=>x.textContent=count)};badge();document.addEventListener('pulso:cart',()=>{badge();qa('[data-cart-count]').forEach(x=>{x.animate?.([{transform:'scale(1)'},{transform:'scale(1.35)'},{transform:'scale(1)'}],{duration:260})})});
  if(!q('#cart-drawer')){const aside=document.createElement('aside');aside.className='cart-drawer';aside.id='cart-drawer';aside.setAttribute('aria-hidden','true');const backdrop=document.createElement('button');backdrop.className='drawer-backdrop';backdrop.dataset.drawerClose='';backdrop.setAttribute('aria-label','Close cart');const panel=document.createElement('div');panel.className='drawer-panel';panel.setAttribute('role','dialog');panel.setAttribute('aria-modal','true');const head=document.createElement('div');head.className='drawer-head';const title=document.createElement('h2');title.textContent='Added to your bag.';const close=document.createElement('button');close.className='drawer-close';close.dataset.drawerClose='';close.setAttribute('aria-label','Close');close.textContent='×';head.append(title,close);const items=document.createElement('div');items.dataset.drawerItems='';const actions=document.createElement('div');actions.className='drawer-actions';const total=document.createElement('div');total.className='drawer-total';const totalLabel=document.createElement('span');totalLabel.textContent='Subtotal';const totalValue=document.createElement('span');totalValue.dataset.drawerTotal='';total.append(totalLabel,totalValue);const continueButton=document.createElement('button');continueButton.className='btn btn--outline';continueButton.dataset.drawerClose='';continueButton.textContent='Continue shopping';const cartLink=document.createElement('a');cartLink.className='btn btn--wine';cartLink.href=window.PulsoPath?window.PulsoPath('/cart/'):'cart/';cartLink.textContent='View cart';actions.append(total,continueButton,cartLink);panel.append(head,items,actions);aside.append(backdrop,panel);document.body.append(aside)}
  const drawer=q('#cart-drawer');let returnFocus=null;
  const closeDrawer=()=>{drawer?.classList.remove('is-open');drawer?.setAttribute('aria-hidden','true');document.body.classList.remove('is-locked');returnFocus?.focus()};
  const renderDrawer=()=>{if(!drawer)return;const items=window.PulsoCart.get();const host=q('[data-drawer-items]',drawer);const latest=items.at(-1);host.textContent='';if(latest){const wrap=document.createElement('div');wrap.className='drawer-item';const img=document.createElement('img');img.src=latest.image;img.alt='';const copy=document.createElement('div');const title=document.createElement('h3');title.textContent=latest.name;const meta=document.createElement('p');meta.textContent=(latest.variant||'Configured product')+' · Qty '+latest.quantity;const price=document.createElement('strong');price.textContent=window.PulsoConfig.money(latest.price*latest.quantity);copy.append(title,meta,price);wrap.append(img,copy);host.append(wrap)}q('[data-drawer-total]',drawer).textContent=window.PulsoConfig.money(window.PulsoCart.subtotal())};
  const openDrawer=(trigger)=>{if(!drawer)return;returnFocus=trigger||document.activeElement;renderDrawer();drawer.classList.add('is-open');drawer.setAttribute('aria-hidden','false');document.body.classList.add('is-locked');q('.drawer-close',drawer)?.focus()};window.openCartDrawer=openDrawer;
  qa('[data-cart-open]').forEach(b=>b.addEventListener('click',()=>openDrawer(b)));qa('[data-drawer-close]').forEach(b=>b.addEventListener('click',closeDrawer));
  document.addEventListener('click',e=>{const add=e.target.closest('[data-add-product]');if(!add)return;const id=add.dataset.id||'pulso-massage-gun',bundle=(window.SITE_CONFIG.bundles||[]).find(x=>x.id===id),p=window.SITE_CONFIG.product,offer=add.hasAttribute('data-offer')&&window.SITE_CONFIG.offer?.enabled;window.PulsoCart.add(bundle?{id:bundle.id,sku:bundle.sku,name:bundle.name,price:bundle.price,image:bundle.image,variant:'Bundle'}:{id:offer?p.id+'-offer':p.id,sku:p.sku,name:p.name,price:offer?window.SITE_CONFIG.offer.price:p.price,image:p.image,variant:offer?'Limited-Time Offer · '+(add.dataset.variant||'BALANCE'):(add.dataset.variant||'BALANCE')});openDrawer(add)});
  document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeDrawer();closeModal()}if(e.key==='Tab'){const panel=q('.is-open .drawer-panel');if(!panel)return;const focusable=qa('button:not([disabled]),a[href],input:not([disabled]),select:not([disabled]),textarea:not([disabled])',panel).filter(x=>x.offsetParent!==null);if(!focusable.length)return;const first=focusable[0],last=focusable.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}});
  const modal=q('#quote-modal');const closeModal=()=>{modal?.classList.remove('is-open');modal?.setAttribute('aria-hidden','true');document.body.classList.remove('is-locked')};window.openQuoteModal=()=>{if(!modal)return;modal.classList.add('is-open');modal.setAttribute('aria-hidden','false');document.body.classList.add('is-locked');q('.form-field input',modal)?.focus()};qa('[data-modal-close]').forEach(b=>b.addEventListener('click',closeModal));
  qa('.aos').forEach(el=>el.setAttribute('data-aos','fade-up'));if(window.AOS)window.AOS.init({once:true,duration:600});else qa('.aos').forEach(el=>el.classList.add('is-visible'));
  document.addEventListener('error',e=>{if(e.target instanceof HTMLImageElement&&!e.target.dataset.fallbackApplied){e.target.dataset.fallbackApplied='true';e.target.classList.add('is-fallback');e.target.alt=e.target.alt||'Image unavailable'}},true);
  if(matchMedia('(prefers-reduced-motion: no-preference)').matches&&innerWidth>760){const pars=qa('[data-parallax]');addEventListener('scroll',()=>requestAnimationFrame(()=>pars.forEach(el=>el.style.transform=`translateY(${(el.getBoundingClientRect().top-innerHeight/2)*-.025}px)`)),{passive:true})}
})();
